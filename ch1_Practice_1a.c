// Q) Write a C program to calculate area of a rectangle using hard coded inputs.

#include <stdio.h>

int main()
{
    int l = 5, b = 7;
    int area = l * b;
    printf("The area of the rectangle is %d . \n", area);
    return 0;
}