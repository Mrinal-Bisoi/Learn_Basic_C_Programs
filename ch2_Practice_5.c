/*  Choose the correct answer :-
    3.0 + 1 will return:
    (a) Integer
    (b) Floating point number
    (c) Character
*/
#include<stdio.h>

int main()
{
    printf("The result of expression 3.0 + 1 is %f \n", 3.0 + 1);
    // The result will return a floating point number means the correct option is b.
    return 0;
}